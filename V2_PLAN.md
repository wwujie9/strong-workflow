# strong-workflow 第二版快速迭代计划

## 版本定位

第一版已经完成消防整改闭环 MVP：群机器人、整改链接、复核链接、上传证据、批量分派、自动催办、PDF 闭环包、上线清单和风险登记表。

第二版目标不是做大而全后台，而是把第一版从“能演示、能试点”推进到“能稳定跑一个真实客户项目，并产出可复用模板”。

## 核心目标

- 让 1 个真实物业/园区/维保项目更快接入。
- 让 30 条真实隐患能从 Excel/维保报告快速导入。
- 让整改链接、复核链接、机器人消息和闭环包更像客户正式交付物。
- 降低试点现场出错概率。
- 为后续正式商用积累数据结构和流程边界。

## 目标客户

- 物业公司工程部。
- 园区安全管理部。
- 消防维保公司项目经理。
- 商业综合体运营/安全负责人。

## 第二版优先级

### P0：试点接入提速

- [x] Excel/CSV 批量导入隐患（M1 已支持 CSV，Excel 可另存 CSV 后导入）。
- [x] 项目初始化配置页：项目名、客户名、维保单位、默认整改期限。
- [x] 责任人/复核人名单配置，不再写死在前端常量。
- [x] 批量导入后预览和确认，避免错误数据直接覆盖。
- [x] 支持从导入数据里自动识别隐患类型和风险等级。

### P0：流程稳定性

- [ ] 后端增加动作级状态机校验，不能只依赖前端保护。
- [ ] 后端增加单条隐患更新接口，减少全量覆盖 JSON 的风险。
- [ ] 自动催办增加频率控制和最近发送时间记录。
- [ ] 机器人发送失败写入日志，前端配置检查页显示最近失败。
- [ ] 增加数据备份接口或备份脚本。

### P1：客户交付体验

- [ ] PDF 闭环包增加封面、项目摘要、维保单位、客户名称。
- [ ] 图片证据在 PDF 打印页中以内嵌缩略图展示。
- [ ] 机器人消息模板优化：分派、整改提交、复核通过、驳回、催办分别更清晰。
- [ ] 复制链接增加“复制群消息文案”，而不只是复制 URL。
- [ ] 首页增加“试点进度”视图：分派数、提交数、复核数、闭环数。

### P1：部署与运维

- [ ] 增加 `scripts/backup.ps1`。
- [ ] 增加 `scripts/restore.ps1`。
- [ ] 增加 `scripts/health-check.ps1`。
- [ ] 增加 Windows 服务或 PM2 启动说明。
- [ ] README 增加公网部署示例：Nginx/Cloudflare Tunnel/Node HTTPS 三种路径。

### P2：正式商用预研

- [ ] 评估 SQLite 或 PostgreSQL 替代 JSON。
- [ ] 设计项目表、隐患表、证据表、操作日志表、机器人消息表。
- [ ] 设计轻量登录和角色权限。
- [ ] 设计多项目隔离。
- [ ] 设计不可篡改审计日志方案。

## 第二版建议里程碑

### M1：导入与项目配置

交付内容：

- [x] CSV 导入，Excel 可先另存为 CSV。
- [x] 项目配置页。
- [x] 人员名单配置。
- [x] 导入预览与追加/替换确认。

验收标准：

- [x] 能用一份客户 CSV 导入 30 条隐患。
- [x] 导入前能预览。
- [x] 导入后责任人和复核人来自配置名单。
- [x] 错误字段会提示，不会静默导入。

### M2：后端状态机与数据安全

交付内容：

- 后端动作级校验。
- 单条隐患更新接口。
- 备份脚本。
- 机器人发送失败记录。

验收标准：

- 直接调用 API 也不能跳过流程规则。
- 坏数据不能覆盖整个隐患文件。
- 能一键备份当前数据和上传文件。
- 机器人失败能被看到。

### M3：客户交付物增强

交付内容：

- PDF 闭环包封面。
- PDF 中展示图片证据。
- 试点进度视图。
- 群消息文案复制。

验收标准：

- 客户能拿 PDF 闭环包做内部汇报。
- 图片证据在 PDF 中可见。
- 项目负责人能一眼看到试点进展。
- 群消息可以直接复制发送。

## 第二版暂不做

- 不做完整 SaaS 多租户。
- 不做复杂审批流设计器。
- 不做 AI 自动识别报告。
- 不做小程序。
- 不做复杂 BI。
- 不做重型账号权限体系。

这些能力等真实试点验证后再进入第三版。

## 技术策略

- 保持 React + Express 架构。
- v2 仍然允许 JSON 存储，但减少全量覆盖风险。
- 导入功能优先支持 CSV，Excel 可通过前端文件读取或后端解析再补。
- 不引入过重依赖，除非能明显降低交付风险。
- 所有新增功能都要保留本地试点可运行，不依赖公网才可开发。

## 验收总标准

- [ ] 使用客户真实 Excel/CSV 能导入 30 条隐患。
- [ ] 批量分派、整改、复核、驳回、补证据、闭环流程稳定。
- [ ] 手机端整改链接可用。
- [ ] 机器人消息可用，失败可见。
- [ ] PDF 闭环包可用于客户汇报。
- [ ] 数据和上传文件可备份。
- [ ] 仍然保持“不替换后台，只嵌入客户现有群工作方式”的产品定位。
